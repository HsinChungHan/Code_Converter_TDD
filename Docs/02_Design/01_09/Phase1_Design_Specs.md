# Phase 1 - Code Converter 設計規格 (2025-01-09 更新)

> **更新日期**：2025-01-09  
> **來源**：Figma 截圖分析  
> **File Key**：`SvcTlADMZ7gUPIa7nN2hT1`  
> **變更原因**：需求調整，簡化 Bookie 選擇流程

---

## ⚠️ 與舊版 (01_01) 的主要差異

| 變更項目 | 舊版 (01_01) | 新版 (01_09) | 影響 |
|----------|--------------|--------------|------|
| **Bookie Dropdown** | ✅ 有 | ❌ 移除 | 元件刪除 |
| **Bookie Selector Bottom Sheet** | ✅ 有（雙欄選擇器） | ❌ 移除 | 元件刪除 |
| **Country 選擇** | ✅ 有 | ❌ 移除 | 流程簡化 |
| **按鈕文字** | "Load" | "Import" | 文字變更 |
| **Placeholder** | "Booking Code" | "Paste any booking code" | 文字變更 |
| **Widget 位置** | 獨立區塊 | Quick Panel 下方 | 佈局變更 |
| **Betslip 行為** | 未明確 | 替換既有 selections | 邏輯確認 |

---

## 📋 目錄

1. [新增/變更元件總覽](#新增變更元件總覽)
2. [Design Tokens](#design-tokens)
3. [Load Code Widget](#load-code-widget)
4. [Toast Message (Partial Error)](#toast-message-partial-error)
5. [Icons](#icons)

---

## 🆕 新增/變更元件總覽

### 全新元件 (New Components)

| 元件名稱 | 說明 | 優先級 | 變更 |
|----------|------|--------|------|
| **Load Code Widget** | 首頁新增的 Booking Code 輸入區塊 | ⭐ 高 | 🔄 簡化版（無 Bookie 選擇） |
| **Toast Message (Partial Error)** | 部分轉換失敗的錯誤提示 | ⭐ 高 | ✅ 保留 |

### ~~已移除元件 (Removed Components)~~

| 元件名稱 | 說明 | 原因 |
|----------|------|------|
| ~~Bookie Selector (Bottom Sheet)~~ | ~~Bookie + Country 雙欄選擇器~~ | 需求簡化 |
| ~~Bookie Dropdown~~ | ~~顯示選中的 Bookie + Country~~ | 需求簡化 |

### 需變更的既有元件 (Existing Components to Modify)

| 元件名稱 | 變更內容 | 優先級 |
|----------|----------|--------|
| **Homepage** | 新增 Load Code Widget 區塊（在 Quick Panel 下方） | ⭐ 高 |
| **Betslip** | 接收轉換後的 selections（**替換**既有 selections） | 中 |

---

## 🎨 Design Tokens (已從 Figma MCP 驗證 ✅)

### 顏色 (Colors)

#### 品牌色 (Brand)

| 變數名稱 | Hex 值 | 用途 | ✅ |
|----------|--------|------|---|
| `brand/brand_secondary` | `#9ff611` | Import 按鈕啟用、Focus 邊框 | ✅ |
| `brand/brand_secondary_disable` | `#343247` | Import 按鈕禁用背景 | ✅ |
| `brand/brand_tertiary` | `#100e26` | Import 按鈕啟用文字 | ✅ |

#### 背景色 (Background)

| 變數名稱 | Hex 值 | 用途 | ✅ |
|----------|--------|------|---|
| `background/type1/background_type1_secondary` | `#100e26` | 輸入框背景 | ✅ |
| `background/type1/background_type1_tertiary` | `#28263c` | Load Code Widget 外框背景 | ✅ |

#### 文字色 (Text)

| 變數名稱 | Hex 值 | 用途 | ✅ |
|----------|--------|------|---|
| `text/type1/text_type1_primary` | `#e7e7e9` | 主要文字、Loading 提示文字 | ✅ |
| `text/type1/text_type1_secondary` | `#878693` | Placeholder 文字、Info Tip Icon | ✅ |
| `text/type1/text_disable_type1_primary` | `#9c9bab` | Import 按鈕禁用文字 | ✅ |

#### 警告/錯誤 (Warning)

| 變數名稱 | Hex 值 | 用途 | ✅ |
|----------|--------|------|---|
| `warning/warning_primary` | `#fb4d3d` | Error 邊框、錯誤文字 | ✅ |

---

### 字型 (Typography) - 已從 Figma 驗證

| 樣式名稱 | Font Family | Weight | Size | Line Height | 用途 | ✅ |
|----------|-------------|--------|------|-------------|------|---|
| `Body/B1_R` | Roboto | Regular (400) | 14px | 21px | 輸入框文字、Placeholder | ✅ |
| `Body/B2_M (btn-s)` | Roboto | Medium (500) | 12px | 16px | Import 按鈕 | ✅ |
| `Body/B2_R` | Roboto | Regular (400) | 12px | auto | 提示文字、錯誤訊息 | ✅ |

---

### 圓角 (Border Radius)

| 元件 | 值 |
|------|-----|
| Load Code Widget 外框 | `10px` |
| 輸入框 | `10px` |
| Import 按鈕 | `2px` |
| Toast Message | `10px` |

---

### 間距 (Spacing)

| 元件 | 屬性 | 值 |
|------|------|-----|
| Load Code Widget | padding | `8px` |
| 輸入框 | padding | `12px 14px` |
| Import 按鈕 | padding | `12px` |
| Import 按鈕 | height | `28px` |
| Toast Message | padding | `12px 16px` |

---

## 🧩 Load Code Widget (新版)

### 元件結構 (已從 Figma MCP 驗證 ✅)

```
Load Code Widget (外框)
├── Input Row
│   ├── Input Container
│   │   ├── Placeholder / 輸入文字
│   │   ├── Clear Icon (⊗) ← Typing/Filled/Error 狀態顯示
│   │   └── Import 按鈕 / Loading Icon
│   └── Info Tip Icon (ⓘ) ← 🆕 新增元件
└── Helper Text (Loading/Error 狀態)
    ├── Loading: "Conversion may take up to 10 seconds..."
    └── Error: "We couldn't find this booking code..."
```

> ⚠️ **與舊版差異**：移除了左側的 Bookie Dropdown，新增 Info Tip Icon

### 狀態對照表

| 狀態 | 輸入框邊框 | Import 按鈕 | 額外元素 |
|------|------------|-------------|----------|
| **Default** | 無邊框 | 灰色禁用 | - |
| **Focus** | `#9ff611` 1px | 灰色禁用 | 綠色游標、鍵盤 |
| **Typing** | `#9ff611` 1px | 綠色啟用 | - |
| **Filled** | 無邊框 | 綠色啟用 | - |
| **Loading** | 無邊框 | Loading icon | 提示文字 |
| **Error** | `#fb4d3d` 1px | 綠色啟用 | 錯誤訊息 |

---

### Default 狀態

```
┌─────────────────────────────────────────────────────────────┐
│  ┌───────────────────────────────────────────────────────┐  │
│  │ Paste any booking code                     ┌────────┐ │  │
│  │                                            │ Import │ │  │
│  └────────────────────────────────────────────└────────┘─┘  │
└─────────────────────────────────────────────────────────────┘
```

**樣式：**
- 外框：`background_type1_tertiary` (#28263c), `border-radius: 10px`, `padding: 8px`
- Input Container：`background_type1_secondary` (#100e26), `border-radius: 10px`, `height: 44px`
- Placeholder：`text_type1_secondary` (#878693), `font: Roboto Medium 14px`
- Import 按鈕：`brand_secondary_disable` (#343247), `text_disable_type1_primary` (#9c9bab)

---

### Focus 狀態

```
┌─────────────────────────────────────────────────────────────┐
│  ┌───────────────────────────────────────────────────────┐  │
│  │ |Paste any booking code                    ┌────────┐ │  │ ← 綠色邊框
│  │                                            │ Import │ │  │
│  └────────────────────────────────────────────└────────┘─┘  │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐    │
│  │              G-Board Alphabetic / English           │    │ ← 鍵盤彈出
│  │  Q W E R T Y U I O P                                │    │
│  │  A S D F G H J K L                                  │    │
│  │  Z X C V B N M ⌫                                    │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

**變化：**
- Input Container：新增 `border: 1px solid #9ff611`
- 游標：`#9ff611`, `width: 1px`
- 鍵盤彈出

---

### Typing 狀態

```
┌─────────────────────────────────────────────────────────────┐
│  ┌───────────────────────────────────────────────────────┐  │
│  │ 3RA3FA                                     ┌────────┐ │  │ ← 綠色邊框
│  │                                            │ Import │ │  │ ← 綠色按鈕
│  └────────────────────────────────────────────└────────┘─┘  │
└─────────────────────────────────────────────────────────────┘
```

**變化：**
- 輸入文字：`text_type1_primary` (#e7e7e9)
- Import 按鈕：`brand_secondary` (#9ff611), 文字 `brand_tertiary` (#100e26)

---

### Loading 狀態

```
┌─────────────────────────────────────────────────────────────┐
│  ┌───────────────────────────────────────────────────────┐  │
│  │ 3RA3FA                                     ┌────────┐ │  │
│  │                                            │   ⟳    │ │  │ ← Loading icon
│  └────────────────────────────────────────────└────────┘─┘  │
│  Conversion may take up to 10 seconds, please stay here and │ ← 提示文字
│  wait for the result.                                       │
└─────────────────────────────────────────────────────────────┘
```

**變化：**
- Import 按鈕：`brand_secondary_disable` (#343247)，內含 Loading icon
- 提示文字：`text_type1_primary` (#e7e7e9), `font: Roboto Regular 12px`

---

### Error 狀態

```
┌─────────────────────────────────────────────────────────────┐
│  ┌───────────────────────────────────────────────────────┐  │
│  │ 3RA3FA                                     ┌────────┐ │  │ ← 紅色邊框
│  │                                            │ Import │ │  │ ← 綠色按鈕
│  └────────────────────────────────────────────└────────┘─┘  │
│  We couldn't find this booking code. Please check and try   │ ← 紅色錯誤文字
│  again.                                                     │
└─────────────────────────────────────────────────────────────┘
```

**變化：**
- Input Container：`border: 1px solid #fb4d3d`
- 錯誤文字：`warning_primary` (#fb4d3d), `font: Roboto Regular 12px`

---

## 🔔 Toast Message (Partial Error) - 已驗證 ✅

> 📍 **node-id**: `26428:71768` (Betslip 畫面底部)

### 樣式規格

```
┌───────────────────────────────────────────────────────────┐
│  ⚠  2 selections failed to convert                       │
└───────────────────────────────────────────────────────────┘
```

**容器：**
- 背景：`warning_primary` (#fb4d3d)
- 圓角：`10px`
- padding：`12px 16px`
- 位置：Betslip 底部（在 "About to pay" 按鈕上方）

**內容：**
- Icon：警告 icon (⚠)，尺寸 `12px`，顏色 `#e7e7e9`
- 文字：`Roboto Regular 12px`，顏色 `#e7e7e9`
- 動態文字：`{N} selections failed to convert`

---

## 🖼️ Icons (已從 Figma MCP 驗證 ✅)

| Icon 名稱 | 用途 | 尺寸 | 顏色 | ✅ |
|-----------|------|------|------|---|
| `icon/tip/1` | Info 提示 icon (輸入框右側) | 16px | `#878693` | ✅ 🆕 |
| `icon/loading` | Import 按鈕 Loading 狀態 | 12px | - | ✅ |
| `Clear Icon` | 清除輸入框按鈕 | 20px | `#878693` | ✅ |
| `icon/exclamation` | Toast 警告 icon | 12px | `#e7e7e9` | - |

---

## 📐 響應式考量

> 所有設計基於 **360px** 寬度的手機畫面

| 元件 | 寬度規則 |
|------|----------|
| Load Code Widget | 滿版 - padding |
| Input Container | `flex: 1` (彈性填滿) |
| Toast Message | 滿版 - `20px` (左右各 10px) |

---

## ⚠️ 業務規則

1. **Betslip 替換規則**：
   - If there are selections in betslip, **replace them**.
   - 轉換成功的 selections 會覆蓋既有的

2. **轉換限制**：
   - **Can only convert football match**
   - 僅支援足球賽事

3. **Bookie 判斷**：
   - ❓ 待 BE 確認：API 如何判斷 Booking Code 來自哪個 Bookie？

---

## ✅ 實作 Checklist

### Load Code Widget
- [ ] 建立 Widget 外框容器
- [ ] 實作 Input Container（**無 Bookie Dropdown**）
- [ ] 實作 6 種狀態切換 (Default/Focus/Typing/Filled/Loading/Error)
- [ ] 實作 Import 按鈕功能
- [ ] 整合鍵盤彈出

### ~~Bookie Selector~~ (已移除)
- ~~實作 Bottom Sheet 容器~~
- ~~實作雙欄選擇器~~

### Toast Message
- [ ] 實作 Toast 容器
- [ ] 整合 Warning icon
- [ ] 實作動態文字（X selections failed）

### Betslip 整合
- [ ] 實作 selections 替換邏輯
- [ ] 處理 Partial 狀態顯示

---

## 📝 待確認事項

1. **Bookie 判斷邏輯**：移除選擇器後，API 如何判斷 Booking Code 來自哪個 Bookie？
2. **API 規格**：BE 討論中，待更新
3. **Info Tip Icon 功能**：點擊後顯示什麼內容？
4. **Betslip Success/Partial node-id**：需補充

---

## ✅ Figma MCP 驗證紀錄

| 項目 | 驗證狀態 | 備註 |
|------|----------|------|
| Load Code Widget Default | ✅ 已驗證 | node-id: 26769:88873 |
| Load Code Widget Loading | ✅ 已驗證 | node-id: 26769:88872 |
| Load Code Widget Error | ✅ 已驗證 | node-id: 26769:88871 |
| Betslip + Partial Toast | ✅ 已驗證 | node-id: 26428:71768 |
| Design Tokens (顏色) | ✅ 已驗證 | 使用 get_variable_defs |
| Design Tokens (字型) | ✅ 已驗證 | Body/B1_R, B2_M, B2_R |
| 新發現元件 | ✅ 已記錄 | icon/tip/1, Clear Icon |

---

*此文件於 2025-01-09 從 Figma MCP 驗證更新*
