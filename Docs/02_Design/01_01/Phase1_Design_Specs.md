# Phase 1 - Code Converter 設計規格

> **生成時間**：2025-01-01  
> **來源**：Figma MCP 自動抓取  
> **File Key**：`SvcTlADMZ7gUPIa7nN2hT1`

---

## 📋 目錄

1. [新增/變更元件總覽](#新增變更元件總覽)
2. [Design Tokens](#design-tokens)
3. [Load Code Widget](#load-code-widget)
4. [Bookie Selector Bottom Sheet](#bookie-selector-bottom-sheet)
5. [Toast Message (Partial Error)](#toast-message-partial-error)
6. [Icons](#icons)

---

## 🆕 新增/變更元件總覽

### 全新元件 (New Components)

| 元件名稱 | 說明 | 優先級 |
|----------|------|--------|
| **Load Code Widget** | 首頁新增的 Booking Code 輸入區塊 | ⭐ 高 |
| **Bookie Selector (Bottom Sheet)** | Bookie + Country 雙欄選擇器 | ⭐ 高 |
| **Toast Message (Partial Error)** | 部分轉換失敗的錯誤提示 | ⭐ 高 |

### 需變更的既有元件 (Existing Components to Modify)

| 元件名稱 | 變更內容 | 優先級 |
|----------|----------|--------|
| **Homepage** | 新增 Load Code Widget 區塊 | ⭐ 高 |
| **Betslip** | 接收轉換後的 selections | 中 |

---

## 🎨 Design Tokens

### 顏色 (Colors)

#### 品牌色 (Brand)

| 變數名稱 | Hex 值 | 用途 |
|----------|--------|------|
| `brand/brand_secondary` | `#9ff611` | 主要 CTA 按鈕、選中狀態、Focus 邊框、游標 |
| `brand/brand_secondary_disable` | `#343247` | 禁用狀態按鈕背景 |
| `brand/brand_tertiary` | `#100e26` | 深色背景、Bottom Sheet 背景 |

#### 背景色 (Background)

| 變數名稱 | Hex 值 | 用途 |
|----------|--------|------|
| `background/type1/background_type1_primary` | `#1c1a31` | 主背景 |
| `background/type1/background_type1_secondary` | `#100e26` | 輸入框背景、Dropdown 背景 |
| `background/type1/background_type1_tertiary` | `#28263c` | Load Code Widget 外框背景 |

#### 文字色 (Text)

| 變數名稱 | Hex 值 | 用途 |
|----------|--------|------|
| `text/type1/text_type1_primary` | `#e7e7e9` | 主要文字（已輸入的 Booking Code） |
| `text/type1/text_type1_secondary` | `#878693` | 次要文字（Placeholder、標籤） |
| `text/type1/text_type1_tertiary` | `#ffffff` | 白色文字（Bookie 選項） |
| `text/type1/text_disable_type1_primary` | `#9c9bab` | 禁用狀態文字 |
| `text/type2/text_type2_secondary` | `#e7e7e9` | Toast 訊息文字 |

#### 線條/邊框 (Line)

| 變數名稱 | Hex 值 | 用途 |
|----------|--------|------|
| `line/type1/line_type1_primary` | `#332f59` | 分隔線 |
| `line/type1/line_type1_secondary` | `#464272` | Bookie 選項未選中背景 (opacity 50%) |

#### 警告/錯誤 (Warning)

| 變數名稱 | Hex 值 | 用途 |
|----------|--------|------|
| `warning/warning_primary` | `#fb4d3d` | 錯誤邊框、錯誤文字、Toast 背景 |

---

### 字型 (Typography)

| 樣式名稱 | Font Family | Weight | Size | Line Height | 用途 |
|----------|-------------|--------|------|-------------|------|
| `Body/B1_M (btn-m)` | Roboto | Medium (500) | 14px | 20px | 中型按鈕、標籤 |
| `Body/B1_B` | Roboto | Bold (700) | 14px | auto | Bookie 選項文字 |
| `Body/B1_R` | Roboto | Regular (400) | 14px | 21px | 一般內文 |
| `Body/B2_M (btn-s)` | Roboto | Medium (500) | 12px | 16px | 小型按鈕 (Load) |
| `Body/B2_R` | Roboto | Regular (400) | 12px | auto | 錯誤訊息、提示文字 |
| `Headline/H4_R` | Roboto | Regular (400) | 16px | auto | 標題 |
| `Sub-Caption/SC1_BI` | Roboto | Bold Italic (700) | 9px | auto | Badge 文字 |

---

### 圓角 (Border Radius)

| 元件 | 值 |
|------|-----|
| Load Code Widget 外框 | `10px` |
| 輸入框 | `10px` |
| Load 按鈕 | `2px` |
| Bottom Sheet 頂部 | `10px` (top-left, top-right) |
| Bookie 選項按鈕 | `10px` |
| Toast Message | `10px` |
| Bottom Sheet 拖曳條 | `18px` |

---

### 間距 (Spacing)

| 元件 | 屬性 | 值 |
|------|------|-----|
| Load Code Widget | padding | `8px` |
| Load Code Widget | gap (內部元素間距) | `8px` |
| 輸入框 | padding | `12px 14px` |
| Load 按鈕 | padding | `12px` |
| Load 按鈕 | height | `28px` |
| Bottom Sheet | padding-x | `12px` |
| Bottom Sheet | padding-top | `6px` |
| Bottom Sheet | padding-bottom | `16px` |
| Bottom Sheet 拖曳條 | width | `60px` |
| Bottom Sheet 拖曳條 | height | `4px` |
| Bookie 選項 | height | `40px` |
| Bookie 選項 | gap (列間距) | `8px` |
| Bookie/Country 欄 | gap (欄間距) | `12px` |
| Toast Message | padding | `12px 16px` |
| Toast Message | gap (icon 與文字) | `4px` |

---

### 陰影 (Shadows)

| 元件 | 值 |
|------|-----|
| Toast Message | `0px 4px 12px rgba(28, 26, 49, 0.25)` |

---

## 🧩 Load Code Widget

### 元件結構

```
Load Code Widget (外框)
├── Bookie Dropdown (左側)
│   ├── Bookie 名稱 (如 "F.com")
│   ├── Country 名稱 (如 "NG")
│   └── 下拉箭頭 icon
└── Input Container (右側)
    ├── Placeholder / 輸入文字
    ├── 游標 (Focus 狀態)
    ├── 清除按鈕 (Typing 狀態)
    └── Load 按鈕
```

### 狀態對照表

| 狀態 | Figma Node | 輸入框邊框 | Load 按鈕 | 額外元素 |
|------|------------|------------|-----------|----------|
| **Default** | `26769:88873` | 無邊框 | 灰色禁用 | - |
| **Focus** | `26769:88868` | `#9ff611` 1px | 灰色禁用 | 綠色游標 |
| **Typing** | `26769:88869` | `#9ff611` 1px | 綠色啟用 | 清除按鈕 ⊗ |
| **Filled** | `26769:88870` | 無邊框 | 綠色啟用 | 清除按鈕 ⊗ |
| **Loading** | `26769:88872` | 無邊框 | Loading icon | 提示文字 |
| **Error** | `26769:88871` | `#fb4d3d` 1px | 綠色啟用 | 錯誤訊息 |

---

### Default 狀態

```
┌─────────────────────────────────────────────────────────────┐
│  ┌──────────────┐  ┌──────────────────────────────────────┐ │
│  │ F.com     ▼  │  │ Booking Code              ┌──────┐  │ │
│  │ NG           │  │                           │ Load │  │ │
│  └──────────────┘  └───────────────────────────└──────┘──┘ │
└─────────────────────────────────────────────────────────────┘
```

**樣式：**
- 外框：`background_type1_tertiary` (#28263c), `border-radius: 10px`, `padding: 8px`
- Bookie Dropdown：`background_type1_secondary` (#100e26), `border-radius: 10px`, `width: 104px`, `height: 44px`
- Input Container：`background_type1_secondary` (#100e26), `border-radius: 10px`, `flex: 1`, `height: 44px`
- Placeholder：`text_type1_secondary` (#878693), `font: Roboto Medium 14px`
- Load 按鈕：`brand_secondary_disable` (#343247), `text_disable_type1_primary` (#9c9bab), `border-radius: 2px`, `height: 28px`

---

### Focus 狀態

```
┌─────────────────────────────────────────────────────────────┐
│  ┌──────────────┐  ┌──────────────────────────────────────┐ │
│  │ F.com     ▼  │  │ |Booking Code              ┌──────┐ │ │ ← 綠色邊框
│  │ NG           │  │                            │ Load │ │ │
│  └──────────────┘  └────────────────────────────└──────┘─┘ │
└─────────────────────────────────────────────────────────────┘
```

**變化：**
- Input Container：新增 `border: 1px solid #9ff611`
- 游標：`#9ff611`, `width: 1px`, `height: 14px`

---

### Typing 狀態

```
┌─────────────────────────────────────────────────────────────┐
│  ┌──────────────┐  ┌──────────────────────────────────────┐ │
│  │ F.com     ▼  │  │ 3RA3FA|        ⊗          ┌──────┐ │ │ ← 綠色邊框
│  │ NG           │  │                            │ Load │ │ │ ← 綠色按鈕
│  └──────────────┘  └────────────────────────────└──────┘─┘ │
└─────────────────────────────────────────────────────────────┘
```

**變化：**
- 輸入文字：`text_type1_primary` (#e7e7e9), `font: Roboto Medium 14px`
- 清除按鈕 ⊗：`size: 20px`, 顏色 `#878693`
- Load 按鈕：`brand_secondary` (#9ff611), 文字 `brand_tertiary` (#100e26)

---

### Loading 狀態

```
┌─────────────────────────────────────────────────────────────┐
│  ┌──────────────┐  ┌──────────────────────────────────────┐ │
│  │ F.com     ▼  │  │ 3RA3FA                    ┌──────┐  │ │
│  │ NG           │  │                           │  ⟳   │  │ │ ← Loading icon
│  └──────────────┘  └───────────────────────────└──────┘──┘ │
│  Conversion may take up to 10 seconds, please stay here... │ ← 提示文字
└─────────────────────────────────────────────────────────────┘
```

**變化：**
- Input Container：移除邊框
- Load 按鈕：`brand_secondary_disable` (#343247)，內含 Loading icon
- 提示文字：`text_type1_primary` (#e7e7e9), `font: Roboto Regular 12px`
- 提示文字內容：`"Conversion may take up to 10 seconds, please stay here and wait for the result."`

---

### Error 狀態

```
┌─────────────────────────────────────────────────────────────┐
│  ┌──────────────┐  ┌──────────────────────────────────────┐ │
│  │ F.com     ▼  │  │ 3RA3FA         ⊗          ┌──────┐ │ │ ← 紅色邊框
│  │ NG           │  │                            │ Load │ │ │ ← 綠色按鈕
│  └──────────────┘  └────────────────────────────└──────┘─┘ │
│  We couldn't find this booking code on [BookieName]...     │ ← 紅色錯誤文字
└─────────────────────────────────────────────────────────────┘
```

**變化：**
- Input Container：`border: 1px solid #fb4d3d`
- 錯誤文字：`warning_primary` (#fb4d3d), `font: Roboto Regular 12px`
- 錯誤文字內容：`"We couldn't find this booking code on [BookieName]. Please check and try again."`

---

## 📱 Bookie Selector Bottom Sheet

### 元件結構

```
Bottom Sheet
├── 拖曳條 (Drag Handle)
├── 標題列
│   ├── "Bookie" 標籤
│   └── "Country" 標籤
├── 雙欄選擇器
│   ├── Bookie 列表 (左欄)
│   └── Country 列表 (右欄)
└── 漸層遮罩 (Fade Out Mask)
```

### Figma Node IDs

| 場景 | Node ID |
|------|---------|
| Scroll Bookie List | `26748:63554` |
| Bookie with Country | `26748:63712` |

### 樣式規格

**Bottom Sheet 容器：**
- 背景：`brand_tertiary` (#100e26)
- 圓角：`10px` (top-left, top-right only)
- padding：`6px 12px 16px`

**拖曳條：**
- 顏色：`#d9d9d9`
- 尺寸：`60px × 4px`
- 圓角：`18px`

**標籤：**
- 字型：`Roboto Medium 14px`
- 顏色：`text_type1_secondary` (#878693)

**Bookie 選項按鈕：**
- 高度：`40px`
- 圓角：`10px`
- 間距：`8px` (垂直)

| 狀態 | 背景色 | 文字色 |
|------|--------|--------|
| 未選中 | `line_type1_secondary` (#464272) opacity 50% | `text_type1_tertiary` (#ffffff) |
| 選中 | `brand_secondary` (#9ff611) | `line_type1_secondary` (#464272) |

**漸層遮罩：**
- 方向：從下到上
- 顏色：`brand_tertiary` (#100e26) 0% → 100%
- 高度：`12px`

---

### Bookie 清單

| Bookie | 支援的 Country | 選擇行為 |
|--------|----------------|----------|
| F.com | NG, GH | 需選 Country |
| Sporty | NG, GH | 需選 Country |
| MSport | NG, TZ | 需選 Country |
| Bet9ja | NG | ✅ 自動選 NG 並關閉 |
| Bangbet | NG, GH, TZ, KE, UG | 需選 Country |
| BetKing | NG | ✅ 自動選 NG 並關閉 |
| 1xBet | NG | ✅ 自動選 NG 並關閉 |
| Betway | NG | ✅ 自動選 NG 並關閉 |
| Stake | - | ❓ 待確認 |
| Bet365 | - | ❓ 待確認 |
| BetPawa | - | ❓ 待確認 |

---

## 🔔 Toast Message (Partial Error)

### Figma Node ID

`26428:71769`

### 樣式規格

```
┌───────────────────────────────────────────────────────────┐
│  ⚠  2 selections failed to convert                       │
└───────────────────────────────────────────────────────────┘
```

**容器：**
- 背景：`warning_primary` (#fb4d3d)
- 圓角：`10px`
- padding：`12px 16px`
- 陰影：`0px 4px 12px rgba(28, 26, 49, 0.25)`
- 位置：螢幕底部，距離左右 `10px`

**內容：**
- Icon：`icon/exclamtion/2`，尺寸 `12px`，顏色 `#e7e7e9`
- 文字：`Roboto Regular 12px`，顏色 `text_type2_secondary` (#e7e7e9)
- Icon 與文字間距：`4px`

---

## 🖼️ Icons

### 使用的 Icons

| Icon 名稱 | 用途 | 尺寸 |
|-----------|------|------|
| `icon/arrow_3/down` | Bookie Dropdown 下拉箭頭 | 12px |
| `icon/loading` | Load 按鈕 Loading 狀態 | 12px |
| `icon/cancel` (Clear Icon) | 清除輸入框 | 20px |
| `icon/exclamtion/2` | Toast 警告 icon | 12px |

### Icon 顏色

| Icon | 狀態 | 顏色 |
|------|------|------|
| 下拉箭頭 | Default | `#ffffff` |
| 清除按鈕 | - | `#878693` |
| 警告 icon | - | `#e7e7e9` |

---

## 📐 響應式考量

> ⚠️ 所有設計基於 **360px** 寬度的手機畫面

| 元件 | 寬度規則 |
|------|----------|
| Load Code Widget | 滿版 - padding |
| Bookie Dropdown | 固定 `104px` |
| Input Container | `flex: 1` (彈性填滿) |
| Bottom Sheet | 滿版 |
| Toast Message | 滿版 - `20px` (左右各 10px) |

---

## ✅ 實作 Checklist

### Load Code Widget
- [ ] 建立 Widget 外框容器
- [ ] 實作 Bookie Dropdown（含下拉箭頭）
- [ ] 實作 Input Container
- [ ] 實作 6 種狀態切換 (Default/Focus/Typing/Filled/Loading/Error)
- [ ] 整合清除按鈕功能
- [ ] 整合 Load 按鈕功能

### Bookie Selector
- [ ] 實作 Bottom Sheet 容器
- [ ] 實作拖曳條
- [ ] 實作雙欄選擇器
- [ ] 實作 Bookie 列表（含捲動）
- [ ] 實作 Country 列表（動態顯示）
- [ ] 實作選中狀態切換
- [ ] 實作漸層遮罩
- [ ] 實作點擊 mask 關閉行為

### Toast Message
- [ ] 實作 Toast 容器
- [ ] 整合 Warning icon
- [ ] 實作動態文字（X selections failed）

---

## 📝 待確認事項

1. **Bookie 清單來源**：Hardcoded / Config / API？
2. **Country 清單來源**：Hardcoded / Config / API？
3. **Stake, Bet365, BetPawa 的 Country 資料**
4. **若 Bookie 無任何 Country 支援，UI 行為為何？**
5. **1.0.7 (Convert Success) 畫面是否存在？**
6. **"Can only convert football match" 限制如何在 UI 上呈現？**

---

*此文件由 Figma MCP 自動生成，如有疑問請參考 Figma 原始設計。*

